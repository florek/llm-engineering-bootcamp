# Wynik quizu — 17.06.2026

## Wynik

- Poprawne odpowiedzi: 17/20
- Skuteczność: 85%

## Szczegóły

| # | Twoja odpowiedź | Poprawna odpowiedź | Wynik | Pewność | Komentarz |
|---|---|---|---|---|---|
| 1 | B | B | OK | | Poprawnie — domyślny port serwera Ollama to 11434. |
| 2 | D | D | OK | | Poprawnie — endpoint kompatybilny z OpenAI to `/v1` na localhost:11434. |
| 3 | A | A | OK | | Poprawnie — SDK wymaga `api_key`, Ollama go nie weryfikuje. |
| 4 | C | C | OK | | Poprawnie — frontier to modele chmurowe, open-source uruchamia się lokalnie. |
| 5 | B | B | OK | | Poprawnie — model trzeba wcześniej pobrać poleceniem `ollama pull`. |
| 6 | A | A | OK | | Poprawnie — rola `system` ustawia globalne instrukcje dla modelu. |
| 7 | — | C | BRAK | | Brak poprawnej odpowiedzi — `raise_for_status()` rzuca wyjątek przy HTTP 4xx/5xx. |
| 8 | D | D | OK | | Poprawnie — własny User-Agent zmniejsza ryzyko blokady przez serwer. |
| 9 | B | B | OK | | Poprawnie — przed wysłaniem do modelu usuwane są `script`, `style`, `noscript`. |
| 10 | A | A | OK | | Poprawnie — odpowiedź modelu to `response.choices[0].message.content`. |
| 11 | D | D | OK | | Poprawnie — obcinanie wynika z ograniczonego okna kontekstu lokalnego modelu. |
| 12 | C | C | OK | | Poprawnie — RAG wzbogaca odpowiedzi o dane z zewnętrznych źródeł. |
| 13 | B | B | OK | | Poprawnie — przed wywołaniem z kodu musi działać `ollama serve`. |
| 14 | A | A | OK | | Poprawnie — przełączenie modelu to zmiana tylko parametru `model`. |
| 15 | B | B | OK | | Poprawnie — SPA renderuje treść przez JS, więc scraping często zwraca pusty HTML. |
| 16 | C | C | OK | | Poprawnie — QLoRA to fine-tuning open-source przy mniejszym zużyciu pamięci. |
| 17 | — | B | BRAK | | Brak odpowiedzi — Kubernetes nie należy do głównych technologii kursu. |
| 18 | A | A | OK | | Poprawnie — bez `base_url` SDK łączy się z chmurą OpenAI. |
| 19 | C | D | BŁĄD | | Wybrano `xml` zamiast `html.parser`, który jest wbudowany w Pythona. |
| 20 | C | C | OK | | Poprawnie — function calling pozwala modelowi wywołać zewnętrzne narzędzie. |

## Mocne strony

- Ollama: port, endpoint API, `ollama pull`, konieczność uruchomionego serwera.
- OpenAI SDK: `api_key`, rola `system`, odczyt odpowiedzi, brak `base_url`.
- Web scraping: User-Agent, tagi do usunięcia, ograniczenia SPA.
- Koncepcje LLM: RAG, QLoRA, function calling, frontier vs open-source.

## Obszary do powtórki

- Metody biblioteki `requests` — szczególnie `raise_for_status()` i reakcja na błędy HTTP.
- Technologie wymienione w kursie — rozróżnianie tego, co jest w programie nauki, a co nie.
- BeautifulSoup — wybór parsera i jego konsekwencje (`html.parser` vs zewnętrzne biblioteki).

## Zagadnienia do utrwalenia

1. `raise_for_status()` — kiedy rzuca wyjątek i dlaczego warto go stosować przy pobieraniu stron.
2. Parser `html.parser` — wbudowany w Pythona, bez dodatkowych zależności systemowych.
3. Główne technologie kursu LLM Engineering — LangChain, Hugging Face, Gradio, RAG, QLoRA (Kubernetes poza zakresem).
4. Różnica między `http://localhost:11434` a `http://localhost:11434/v1` jako `base_url`.
5. Dlaczego obcina się treść strony przed wysłaniem do lokalnego modelu (limit okna kontekstu).
