# Wynik quizu — 18.06.2026

## Wynik

- Poprawne odpowiedzi: 16/20
- Skuteczność: 80%

## Szczegóły

| # | Twoja odpowiedź | Poprawna odpowiedź | Wynik | Pewność | Komentarz |
|---|---|---|---|---|---|
| 1 | B | B | OK | | Poprawnie: model nie ma dostępu do internetu — treść trzeba pobrać, oczyścić i przekazać w prompcie. |
| 2 | X | D | BRAK | | Rola `assistant` przechowuje poprzednie odpowiedzi modelu w historii konwersacji. |
| 3 | A | A | OK | | `html.parser` jest wbudowany w Pythona i nie wymaga dodatkowych zależności systemowych. |
| 4 | C | C | OK | | Inference to podstawowe wywołanie modelu, np. chat completion. |
| 5 | B | B | OK | | `ollama serve` uruchamia serwer, ale bez `ollama pull` wagi modelu nie są dostępne. |
| 6 | D | D | OK | | Bezpieczna obsługa pustej odpowiedzi: `message.content or ""`. |
| 7 | C | C | OK | | Lokalne inference oznacza, że dane nie opuszczają maszyny — lepsza prywatność. |
| 8 | D | A | BŁĄD | | `separator="\n"` oddziela tekst z kolejnych bloków HTML nową linią, a nie łączy linie w jeden ciąg. |
| 9 | D | D | OK | | W produkcji lepsze niż obcięcie od początku są chunking, map-reduce lub RAG. |
| 10 | B | B | OK | | AI Agents łączą LLM z narzędziami, pamięcią i pętlą planowania. |
| 11 | C | C | OK | | Wzorzec produkcyjny: najpierw `system` (reguły), potem `user` (dane wejściowe). |
| 12 | A | A | OK | | `timeout=30` zapobiega wiszącym połączeniom na wolnych serwerach. |
| 13 | X | B | BRAK | | `decompose()` usuwa tag wraz z całym poddrzewem dzieci z drzewa DOM. |
| 14 | D | D | OK | | Function calling pozwala modelowi zdecydować o wywołaniu zewnętrznej funkcji lub narzędzia. |
| 15 | C | C | OK | | Fine-tuning to dostosowanie wag modelu do konkretnego zadania. |
| 16 | A | A | OK | | `deepseek-r1:1.5b` to mniejszy model reasoningowy DeepSeek. |
| 17 | D | D | OK | | Struktura odpowiedzi Ollamy jest identyczna z API OpenAI — ten sam kod parsujący działa w obu przypadkach. |
| 18 | X | B | BRAK | | Generator broszur rozwija pipeline do wielu stron, chunkingu, embeddings, vector DB i strukturalnego raportu. |
| 19 | C | C | OK | | Filtrowanie pustych linii daje czytelniejszy tekst wejściowy dla modelu. |
| 20 | A | A | OK | | Walidacja `urlparse` wymaga scheme (np. https) i netloc (domeny). |

## Mocne strony

- Integracja danych z WWW z LLM — rozumiesz, że model nie ma dostępu do internetu i wymaga przygotowania tekstu.
- OpenAI SDK i Ollama — poprawnie obsługujesz kluczowe koncepcje: `api_key`, `message.content`, kompatybilność API, wzorzec `system` + `user`.
- LLM Engineering — dobra znajomość inference, fine-tuningu, function calling, AI Agents i korzyści lokalnego inference.
- RAG i rozwój pipeline'u — wiesz, że w produkcji lepsze są chunking, map-reduce lub RAG zamiast prostego obcięcia tekstu.
- Python — poprawnie rozróżniasz `html.parser`, `timeout` w requests i walidację URL przez `urlparse`.

## Obszary do powtórki

- Role w tablicy `messages` — szczególnie rola `assistant` vs `system` vs `user`.
- BeautifulSoup — metoda `decompose()` i parametr `separator` w `get_text()`.
- Ewolucja pipeline'u streszczania w kierunku generatora broszur firmowych.

## Zagadnienia do utrwalenia

1. Rola `assistant` w `messages` — przechowuje poprzednie odpowiedzi modelu w historii konwersacji.
2. Metoda `decompose()` — usuwa tag wraz z całym poddrzewem dzieci z drzewa DOM.
3. Parametr `separator="\n"` w `get_text()` — oddziela tekst z kolejnych bloków HTML nową linią.
4. Ewolucja pipeline'u: od streszczenia jednej strony do wielu stron firmy z chunkingiem, embeddings, vector DB i strukturalnym raportem.
5. Różnica między `strip=True` a filtrowaniem pustych linii po `splitlines()` — oba poprawiają czytelność tekstu wejściowego.
