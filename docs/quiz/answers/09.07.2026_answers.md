# Klucz odpowiedzi — 09.07.2026

1. C — Serwer Ollama domyślnie nasłuchuje na porcie 11434.
2. D — RAG to Retrieval-Augmented Generation — wzbogacanie odpowiedzi modelu o dane z zewnętrznych źródeł.
3. A — OpenAI SDK wymaga parametru `api_key`, ale Ollama nie weryfikuje jego wartości — wystarczy dowolny string.
4. B — Podstawowy pipeline GenAI: przygotuj dane → zbuduj prompt → wywołaj model → odczytaj wynik.
5. D — Modele frontier wymagają klucza API, działają w chmurze i generują koszty per token.
6. C — Przed ekstrakcją tekstu usuwa się tagi `script`, `style` i `noscript` metodą `decompose()`.
7. B — Gdy modele zbliżają się jakościowo, w produkcji coraz ważniejsze stają się cena i szybkość (latency).
8. A — Wymuszenie wyjaśnienia strategii aktywuje głębszy tryb rozumowania i poprawia jakość złożonych decyzji.
9. C — Model reasoningowy poświęca więcej czasu na wewnętrzne rozumowanie przed udzieleniem odpowiedzi.
10. D — Rola `system` ustawia globalne instrukcje dotyczące tonu, formatu i języka odpowiedzi modelu.
11. B — Embeddings i baza wektorowa pozwalają dołączyć do promptu tylko najistotniejsze fragmenty dokumentu.
12. A — Walidacja URL wymaga obecności pól `scheme` (np. https) i `netloc` (domena).
13. C — Modularny pipeline dzieli się na warstwę danych, warstwę LLM i warstwę orchestracji.
14. D — Model hybrydowy łączy zalety modeli chatowych i reasoningowych w jednej ofercie.
15. B — Model bazowy nie jest dostrojony do dialogu — wymaga instrukcji lub fine-tuningu, by prowadzić rozmowę.
16. A — Przy function calling model decyduje, kiedy wywołać zewnętrzną funkcję lub narzędzie.
17. C — Obcięcie od początku do limitu znaków powoduje utratę końcówki długiego artykułu.
18. B — Przed wywołaniem musi działać serwer (`ollama serve`) i model musi być pobrany (`ollama pull`).
19. D — Bez limitu budżetu myślenia model reasoningowy może odpowiadać znacznie dłużej — orchestrator czeka na wolniejszych.
20. A — Sojusz powstaje, gdy dwóch graczy wspólnie atakuje tego samego przeciwnika i wzajemnie przekazuje sobie jednostki — wtedy otrzymują bonus.
