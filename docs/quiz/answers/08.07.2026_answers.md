# Klucz odpowiedzi — 08.07.2026

1. **C** — Serwer Ollama domyślnie nasłuchuje na porcie 11434.
2. **D** — RAG to Retrieval-Augmented Generation — wzbogacanie odpowiedzi modelu o dane z zewnętrznych źródeł.
3. **A** — Parametr `api_key` jest wymagany przez OpenAI SDK, ale Ollama nie weryfikuje przekazanej wartości.
4. **B** — `raise_for_status()` rzuca wyjątek przy kodach HTTP 4xx/5xx zamiast cichego przetwarzania błędu.
5. **D** — Podstawowy pipeline GenAI: przygotuj dane → zbuduj prompt → wywołaj model → odczytaj wynik.
6. **A** — Modele frontier wymagają klucza API i generują koszty per token.
7. **C** — Przed ekstrakcją tekstu usuwane są tagi `script`, `style` i `noscript` metodą `decompose()`.
8. **B** — Rola `user` służy do przekazania pytania lub danych wejściowych od użytkownika.
9. **A** — Embeddings i baza wektorowa pozwalają dołączyć do promptu tylko najistotniejsze fragmenty dokumentu.
10. **D** — `decompose()` trwale usuwa tag wraz z dziećmi z drzewa DOM przed ekstrakcją tekstu, w przeciwieństwie do samego wyciągnięcia tekstu.
11. **B** — Obcięcie od początku strony zachowuje początek artykułu, a końcówka długiego tekstu ginie.
12. **B** — Walidacja URL wymaga obecności `scheme` (np. `https`) i `netloc` (domeny).
13. **D** — Function calling pozwala modelowi zdecydować, kiedy wywołać zewnętrzną funkcję lub narzędzie — w przeciwieństwie do zwykłego inference zwracającego tylko tekst.
14. **A** — Przed pierwszym wywołaniem muszą być wykonane oba kroki: uruchomienie serwera i pobranie modelu; kolejność nie ma znaczenia.
15. **C** — Bez `base_url` SDK łączy się z chmurą OpenAI i wymaga prawdziwego klucza API.
16. **B** — Parametr `strip=True` obcina białe znaki z każdego wyciągniętego fragmentu tekstu.
17. **D** — Modularny pipeline dzieli się na warstwę danych, warstwę LLM i warstwę orchestracji.
18. **A** — Pusty tekst kończy się `ValueError`, aby nie wysyłać pustego promptu — model bez danych może halucynować.
19. **B** — Filtrowanie pustych linii daje czytelniejszy tekst wejściowy dla modelu.
20. **C** — Ollama wystawia endpoint kompatybilny z OpenAI Chat Completions, co umożliwia użycie tego samego kodu SDK.
