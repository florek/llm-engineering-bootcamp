# Klucz odpowiedzi — 07.07.2026

1. **B** — Ollama wystawia API kompatybilne z OpenAI pod adresem `http://localhost:11434/v1`, który przekazuje się jako `base_url`.
2. **C** — Metoda `decompose()` usuwa tag wraz z dziećmi z drzewa DOM przed ekstrakcją tekstu.
3. **A** — Fine-tuning to dostosowanie wag modelu do konkretnego zadania, np. z użyciem QLoRA dla modeli open-source.
4. **D** — AI Agents to autonomiczne systemy łączące LLM z narzędziami, pamięcią i pętlą planowania.
5. **B** — Parametr `timeout` zapobiega wiszącym połączeniom, gdy serwer odpowiada zbyt wolno lub w ogóle nie odpowiada.
6. **C** — Rola `system` ustawia globalne instrukcje dla modelu, np. język i format odpowiedzi.
7. **A** — Parser `html.parser` jest wbudowany w Pythona i nie wymaga dodatkowych zależności systemowych.
8. **D** — Embeddings reprezentują fragmenty tekstu jako wektory, co umożliwia wyszukiwanie podobieństwa w bazie wektorowej.
9. **B** — Lokalne modele open-source nie generują opłat per token i dane nie opuszczają maszyny użytkownika.
10. **C** — Pole `content` może być `None`; wzorzec `or ""` chroni przed błędem przy dalszym przetwarzaniu.
11. **A** — Inference to podstawowe wywołanie modelu: wysłanie promptu i odczytanie odpowiedzi.
12. **D** — Walidacja URL wymaga obecności `scheme` (np. `https`) i `netloc` (domeny).
13. **B** — `deepseek-r1:1.5b` to mniejszy model reasoningowy; ćwiczenie pokazuje przełączanie modeli bez zmiany reszty kodu.
14. **C** — Ścieżka nauki: inference → integracja z danymi → fine-tuning → wdrożenie → agenci AI.
15. **A** — Bez wiadomości systemowej model może odpowiedzieć w złym języku lub nieoczekiwanym formacie.
16. **D** — Wiele serwerów blokuje domyślny User-Agent biblioteki `requests`; własny UA zmniejsza ryzyko 403.
17. **B** — Rola `assistant` przechowuje poprzednie odpowiedzi modelu w historii wieloetapowej konwersacji.
18. **A** — Embeddings i baza wektorowa pozwalają dołączyć do promptu tylko najistotniejsze fragmenty zamiast całego dokumentu.
19. **C** — `ollama pull` pobiera pliki wag modelu na dysk; samo `ollama serve` uruchamia tylko serwer HTTP.
20. **D** — Parametr `separator="\n"` oddziela każdy blok HTML nową linią, co daje czytelniejszy tekst wejściowy.
