# Klucz odpowiedzi — 17.06.2026

1. **B** — Serwer Ollama domyślnie nasłuchuje na porcie 11434.
2. **D** — Endpoint kompatybilny z OpenAI Chat Completions to `http://localhost:11434/v1`.
3. **A** — OpenAI SDK wymaga parametru `api_key`, ale Ollama go nie weryfikuje — wystarczy dowolna wartość.
4. **C** — Modele frontier to duże modele komercyjne w chmurze; open-source można uruchomić lokalnie.
5. **B** — Przed użyciem modelu trzeba go pobrać poleceniem `ollama pull`.
6. **A** — Rola `system` ustawia globalne instrukcje (ton, język, format odpowiedzi).
7. **C** — `raise_for_status()` rzuca wyjątek przy kodach HTTP 4xx/5xx zamiast cicho przetwarzać błąd.
8. **D** — Wiele serwerów blokuje domyślny User-Agent biblioteki requests; własny UA zmniejsza ryzyko odrzucenia.
9. **B** — Tagi `script`, `style` i `noscript` nie niosą treści merytorycznej i są usuwane przed wysłaniem do modelu.
10. **A** — Treść odpowiedzi modelu to `response.choices[0].message.content`.
11. **D** — Lokalne modele mają ograniczone okno kontekstu — długi tekst trzeba obcinać przed wysłaniem.
12. **C** — RAG wzbogaca odpowiedzi modelu o dane z zewnętrznych źródeł (dokumenty, bazy wektorowe).
13. **B** — Serwer Ollama (`ollama serve`) musi działać przed wywołaniem z kodu Pythona.
14. **A** — Przełączenie modelu wymaga zmiany tylko parametru `model` — reszta kodu pozostaje bez zmian.
15. **B** — Strony SPA często zwracają pusty HTML, bo treść jest renderowana przez JavaScript po stronie klienta.
16. **C** — QLoRA to technika fine-tuningu modeli open-source przy mniejszym zużyciu pamięci.
17. **B** — Kubernetes nie jest wymieniony w głównych technologiach kursu (są m.in. LangChain, Hugging Face, Gradio).
18. **A** — Bez `base_url` SDK łączy się z chmurą OpenAI i wymaga prawdziwego klucza API.
19. **D** — Parser `html.parser` jest wbudowany w Pythona i nie wymaga dodatkowych zależności systemowych.
20. **C** — Function calling pozwala modelowi zdecydować, kiedy wywołać zewnętrzną funkcję lub narzędzie.
