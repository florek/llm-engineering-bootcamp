# Klucz odpowiedzi — 06.07.2026

1. **C** — RAG to Retrieval-Augmented Generation: wzbogacanie odpowiedzi modelu o dane z zewnętrznych źródeł.
2. **A** — Bez `base_url` SDK łączy się z chmurą OpenAI i wymaga prawdziwego klucza API.
3. **D** — Serwer Ollama domyślnie nasłuchuje na porcie 11434.
4. **B** — Modele frontier wymagają klucza API i generują koszty per token.
5. **C** — `raise_for_status()` rzuca wyjątek przy kodach HTTP 4xx/5xx zamiast cichego przetwarzania błędu.
6. **A** — Parametr `api_key` jest wymagany przez SDK, ale Ollama nie weryfikuje jego wartości.
7. **B** — Przed wysłaniem do modelu usuwane są tagi `script`, `style` i `noscript`.
8. **D** — Multimodal AI obejmuje modele przetwarzające tekst, obraz, audio itd.
9. **C** — W ćwiczeniu streszczania treść jest obcinana do 12 000 znaków przed wysłaniem do modelu.
10. **A** — Treść odpowiedzi odczytuje się przez `response.choices[0].message.content`.
11. **D** — QLoRA to technika fine-tuningu stosowana przy modelach open-source.
12. **B** — Walidacja `urlparse` wymaga scheme i netloc — string bez protokołu nie przejdzie.
13. **C** — `llama3.2` to lekki model Meta odpowiedni do chatu i streszczania.
14. **A** — Podstawowy pipeline GenAI: przygotuj dane → zbuduj prompt → wywołaj model → zwróć wynik.
15. **D** — Obcięcie od początku gubi końcówkę artykułu; w produkcji lepsze są chunking, map-reduce lub RAG.
16. **B** — Function calling oznacza, że model decyduje, kiedy wywołać zewnętrzną funkcję lub narzędzie.
17. **C** — SPA często zwracają pusty HTML, bo treść renderuje JavaScript po stronie klienta.
18. **A** — Przełączenie modelu wymaga zmiany wyłącznie parametru `model` — reszta kodu pozostaje bez zmian.
19. **D** — Bez walidacji pustej treści model może wygenerować halucynację na podstawie pustego promptu.
20. **B** — Generator broszur rozwija pipeline do wielu stron, chunkingu, embeddings, vector DB i strukturalnego raportu.
