# Klucz odpowiedzi — 18.06.2026

1. **B** — Model nie ma dostępu do internetu; treść strony trzeba pobrać, oczyścić z HTML i przekazać w prompcie.
2. **D** — Rola `assistant` przechowuje poprzednie odpowiedzi modelu w historii konwersacji.
3. **A** — Parser `html.parser` jest wbudowany w Pythona i nie wymaga dodatkowych zależności systemowych.
4. **C** — Inference to podstawowe wywołanie modelu, np. chat completion.
5. **B** — `ollama serve` uruchamia serwer, ale bez `ollama pull` wagi modelu nie są dostępne do inference.
6. **D** — `message.content` może być `None`; bezpieczna obsługa to `content or ""`.
7. **C** — Lokalne inference oznacza, że dane nie opuszczają maszyny — lepsza prywatność.
8. **A** — Parametr `separator="\n"` oddziela tekst z kolejnych bloków HTML nową linią.
9. **D** — W produkcji lepsze niż obcięcie od początku są chunking, map-reduce lub RAG.
10. **B** — AI Agents to autonomiczne systemy łączące LLM z narzędziami, pamięcią i pętlą planowania.
11. **C** — Typowy wzorzec produkcyjny: najpierw `system` (reguły), potem `user` (dane wejściowe).
12. **A** — `timeout=30` zapobiega wiszącym połączeniom na wolnych lub nieodpowiadających serwerach.
13. **B** — `decompose()` usuwa tag wraz z całym poddrzewem dzieci z drzewa DOM.
14. **D** — Function calling pozwala modelowi zdecydować, kiedy wywołać zewnętrzną funkcję lub narzędzie.
15. **C** — Fine-tuning to dostosowanie wag modelu do konkretnego zadania.
16. **A** — `deepseek-r1:1.5b` to mniejszy model reasoningowy DeepSeek używany do testów przełączania modeli.
17. **D** — Struktura odpowiedzi Ollamy jest identyczna z API OpenAI — ten sam kod parsujący działa w obu przypadkach.
18. **B** — Generator broszur rozwija pipeline do wielu stron, chunkingu, embeddings, vector DB i strukturalnego raportu.
19. **C** — Filtrowanie pustych linii daje czytelniejszy tekst wejściowy dla modelu.
20. **A** — Walidacja `urlparse` wymaga obecności scheme (np. https) i netloc (domeny).
