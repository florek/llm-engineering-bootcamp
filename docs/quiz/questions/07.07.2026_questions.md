# Quiz — 07.07.2026

## Pytania

**1.** [Narzędzia] Jaki adres `base_url` ustawia się w OpenAI SDK przy pracy z lokalną Ollamą?

A) `https://api.openai.com/v1`  
B) `http://localhost:11434/v1`  
C) `http://localhost:8080/chat`  
D) `http://127.0.0.1:5000/api`  

**2.** [Python] Co robi metoda `decompose()` na tagu BeautifulSoup?

A) Zamienia tag na czysty tekst bez usuwania go z drzewa DOM  
B) Ukrywa tag wizualnie w renderowanym HTML  
C) Usuwa tag wraz z dziećmi z drzewa DOM  
D) Konwertuje tag na komentarz HTML  

**3.** [LLM] Czym jest fine-tuning w kontekście kursu?

A) Dostosowanie wag modelu do konkretnego zadania  
B) Pobieranie wag modelu poleceniem `ollama pull`  
C) Obcinanie tekstu wejściowego do limitu znaków  
D) Walidacja adresu URL przed scrapowaniem strony  

**4.** [LLM] Czym są AI Agents według notatek z kursu?

A) Wyłącznie modele do generowania embeddings  
B) Biblioteka do parsowania HTML stron WWW  
C) Narzędzie do uruchamiania serwera Ollama  
D) Autonomiczne systemy łączące LLM z narzędziami, pamięcią i pętlą planowania  

**5.** [Python] Po co ustawia się parametr `timeout` przy `requests.get()` podczas pobierania strony?

A) Aby automatycznie walidować poprawność URL  
B) Aby zapobiec wiszącym połączeniom na wolnej stronie  
C) Aby wymusić kodowanie UTF-8 odpowiedzi serwera  
D) Aby pobrać pliki wag modelu z Ollamy  

**6.** [API] Która rola w tablicy `messages` ustawia globalne instrukcje dotyczące języka i formatu odpowiedzi?

A) `assistant`  
B) `user`  
C) `system`  
D) `tool`  

**7.** [Python] Dlaczego parser `html.parser` jest preferowany w ćwiczeniu scrapingu?

A) Jest wbudowany w Pythona i nie wymaga dodatkowych zależności systemowych  
B) Obsługuje renderowanie JavaScript po stronie klienta  
C) Automatycznie tłumaczy HTML na markdown  
D) Jest wymagany przez OpenAI SDK do parsowania odpowiedzi modelu  

**8.** [RAG] Jaka jest rola embeddings w pipeline RAG?

A) Zastępują potrzebę czyszczenia HTML przed wysłaniem do modelu  
B) Służą wyłącznie do walidacji adresu URL  
C) Konwertują odpowiedź modelu na plik PNG  
D) Reprezentują fragmenty tekstu jako wektory do wyszukiwania podobieństwa w bazie wektorowej  

**9.** [LLM] Która zaleta dotyczy modeli open-source uruchamianych lokalnie przez Ollamę?

A) Zawsze mają większe okno kontekstu niż modele frontier  
B) Dane nie opuszczają maszyny i nie ma opłat per token  
C) Nie wymagają własnego sprzętu komputerowego  
D) Automatycznie mają dostęp do internetu w czasie rzeczywistym  

**10.** [API] Dlaczego warto użyć wzorca `message.content or ""` przy odczycie odpowiedzi modelu?

A) Aby wymusić odpowiedź w języku polskim  
B) Aby zwiększyć okno kontekstu modelu  
C) Bo pole `content` może być `None` i bez tego kod może się wyłożyć  
D) Bo Ollama wymaga pustego stringa jako wartości `api_key`  

**11.** [LLM] Co oznacza inference w kontekście kursu?

A) Podstawowe wywołanie modelu — wysłanie promptu i odczytanie odpowiedzi  
B) Podział dokumentu na fragmenty i zapis do bazy wektorowej  
C) Pobranie strony WWW i usunięcie tagów `script` oraz `style`  
D) Uruchomienie serwera Ollama na porcie 11434  

**12.** [Python] Które pola zwrócone przez `urlparse` muszą być obecne, aby URL przeszedł walidację w pipeline streszczania?

A) `path` i `fragment`  
B) `username` i `password`  
C) `query` i `params`  
D) `scheme` i `netloc`  

**13.** [Narzędzia] Czym charakteryzuje się model `deepseek-r1:1.5b` w ćwiczeniach kursu?

A) Największym modelem frontier dostępnym wyłącznie w chmurze  
B) Mniejszym modelem reasoningowym — pokazuje przełączanie modeli bez zmiany reszty kodu  
C) Modelem do generowania wyłącznie embeddings  
D) Modelem wymagającym innego formatu tablicy `messages` niż OpenAI  

**14.** [LLM] Jaka jest ścieżka nauki opisana w kursie?

A) Wdrożenie → inference → scraping  
B) Fine-tuning → scraping → inference  
C) Inference → integracja z danymi → fine-tuning → wdrożenie → agenci  
D) Wyłącznie multimodal AI bez RAG i fine-tuningu  

**15.** [GenAI] Jaki efekt ma brak wiadomości systemowej przy streszczaniu treści strony?

A) Model może odpowiedzieć w złym języku lub formacie  
B) Ollama automatycznie odmówi wygenerowania odpowiedzi  
C) BeautifulSoup nie sparsuje pobranego HTML  
D) OpenAI SDK połączy się z chmurą zamiast z lokalnym serwerem  

**16.** [Python] Dlaczego własny nagłówek User-Agent zmniejsza ryzyko błędu 403 Forbidden?

A) Bo wymusza połączenie HTTPS zamiast HTTP  
B) Bo automatycznie renderuje JavaScript po stronie klienta  
C) Bo zwiększa wartość parametru `timeout`  
D) Bo wiele serwerów blokuje domyślny User-Agent biblioteki `requests`  

**17.** [API] Do czego służy rola `assistant` w tablicy `messages`?

A) Do przekazywania instrukcji globalnych modelowi  
B) Do przechowywania poprzednich odpowiedzi modelu w historii konwersacji  
C) Do wysyłania surowego HTML pobranego ze strony  
D) Do konfiguracji parametru `base_url` klienta OpenAI  

**18.** [RAG] Dlaczego embeddings i baza wektorowa rozwiązują problem ograniczonego okna kontekstu?

A) Pozwalają wyszukać i dołączyć do promptu tylko najistotniejsze fragmenty dokumentu  
B) Zwiększają fizyczny rozmiar pamięci GPU na maszynie lokalnej  
C) Eliminują potrzebę jakiegokolwiek promptu systemowego  
D) Automatycznie tłumaczą tekst wejściowy na język polski  

**19.** [Narzędzia] Co robi `ollama pull`, czego nie robi samo `ollama serve`?

A) Uruchamia serwer HTTP nasłuchujący na porcie 11434  
B) Weryfikuje klucz API przekazany do OpenAI SDK  
C) Pobiera pliki wag wybranego modelu na dysk  
D) Parsuje HTML stron internetowych przed wysłaniem do modelu  

**20.** [Python] Po co używa się `separator="\n"` w metodzie `soup.get_text()`?

A) Aby usunąć tagi `script`, `style` i `noscript` z drzewa DOM  
B) Aby walidować pola `scheme` i `netloc` adresu URL  
C) Aby wymusić odpowiedź modelu w formacie punktów  
D) Aby każdy blok HTML był oddzielony nową linią przy ekstrakcji tekstu  
