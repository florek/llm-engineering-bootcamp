# Quiz — 08.07.2026

## Pytania

**1.** [Narzędzia] Na jakim porcie domyślnie nasłuchuje serwer Ollama?

A) 8080  
B) 5000  
C) 11434  
D) 3000  

**2.** [LLM] Co oznacza skrót RAG w kontekście LLM Engineering?

A) Recursive API Gateway  
B) Reinforcement-Augmented Generation  
C) Real-time Automated GPT  
D) Retrieval-Augmented Generation  

**3.** [API] Jak działa parametr `api_key` przy połączeniu OpenAI SDK z Ollamą?

A) Jest wymagany przez SDK, ale Ollama nie weryfikuje żadnej wartości  
B) Musi być identyczny z nazwą pobranego modelu  
C) Nie jest potrzebny — SDK działa bez niego  
D) Musi być wygenerowany na oficjalnej stronie Ollamy  

**4.** [Python] Do czego służy `response.raise_for_status()` po pobraniu strony WWW?

A) Konwertuje HTML na czysty tekst przed parsowaniem  
B) Rzuca wyjątek przy kodach HTTP 4xx/5xx  
C) Ustawia nagłówek User-Agent w żądaniu  
D) Waliduje pola `scheme` i `netloc` adresu URL  

**5.** [GenAI] Jaka jest poprawna kolejność kroków w podstawowym pipeline GenAI opisanym w kursie?

A) Wywołaj model → pobierz dane → zbuduj prompt → zwróć wynik  
B) Zbuduj prompt → pobierz dane → wywołaj model → zwróć wynik  
C) Pobierz dane → zwróć wynik → zbuduj prompt → wywołaj model  
D) Przygotuj dane wejściowe → zbuduj prompt → wywołaj model → odczytaj wynik  

**6.** [LLM] Które stwierdzenie opisuje modele frontier?

A) Wymagają klucza API i generują koszty per token  
B) Działają wyłącznie lokalnie przez Ollamę bez opłat  
C) Zawsze mają mniejsze okno kontekstu niż modele open-source  
D) Nie obsługują chat completions  

**7.** [Python] Które tagi HTML są usuwane metodą `decompose()` przed ekstrakcją tekstu dla modelu?

A) `div`, `span`, `p`  
B) `a`, `img`, `table`  
C) `script`, `style`, `noscript`  
D) `header`, `footer`, `nav`  

**8.** [API] Do czego służy rola `user` w tablicy `messages`?

A) Do przechowywania poprzednich odpowiedzi modelu w historii konwersacji  
B) Do przekazania pytania lub danych wejściowych od użytkownika  
C) Do ustawienia globalnych instrukcji dotyczących języka i formatu  
D) Do konfiguracji parametru `base_url` klienta OpenAI  

**9.** [RAG] Dlaczego embeddings i baza wektorowa rozwiązują problem ograniczonego okna kontekstu?

A) Pozwalają wyszukać i dołączyć do promptu tylko najistotniejsze fragmenty dokumentu  
B) Zwiększają fizyczny rozmiar pamięci GPU na maszynie lokalnej  
C) Eliminują potrzebę jakiegokolwiek promptu systemowego  
D) Automatycznie tłumaczą tekst wejściowy na język polski  

**10.** [Python] Czym różni się `decompose()` od samego `get_text()` na tagu BeautifulSoup?

A) `decompose()` ukrywa tag wizualnie, ale zostawia go w drzewie DOM  
B) `get_text()` usuwa tag wraz z dziećmi, a `decompose()` tylko wyciąga tekst  
C) Obie metody robią dokładnie to samo — różni się tylko nazwa  
D) `decompose()` trwale usuwa tag wraz z dziećmi z drzewa DOM przed ekstrakcją tekstu  

**11.** [GenAI] Jaki efekt ma obcięcie tekstu strony od początku do limitu 12 000 znaków przed wysłaniem do modelu?

A) Model automatycznie uzupełnia brakującą końcówkę artykułu  
B) Tracona jest końcówka długiego artykułu, zachowany zostaje początek  
C) Tekst jest dzielony na embeddings i zapisywany w bazie wektorowej  
D) Ollama odmawia wygenerowania odpowiedzi przy przekroczeniu limitu  

**12.** [Python] Które pola zwrócone przez `urlparse` muszą być obecne, aby adres URL przeszedł walidację przed pobraniem strony?

A) `path` i `fragment`  
B) `scheme` i `netloc`  
C) `query` i `params`  
D) `username` i `password`  

**13.** [LLM] Czym różni się function calling od zwykłego inference?

A) Function calling wymaga wyłącznie modeli frontier w chmurze  
B) Inference zawsze zwraca strukturalne żądanie wywołania narzędzia  
C) Function calling automatycznie parsuje HTML stron internetowych  
D) Przy function calling model decyduje, kiedy wywołać zewnętrzną funkcję lub narzędzie  

**14.** [Narzędzia] Co musi być spełnione przed pierwszym wywołaniem chat completion z Ollamą?

A) Serwer musi działać (`ollama serve`) i model musi być pobrany (`ollama pull`) — kolejność tych kroków nie ma znaczenia  
B) Wystarczy samo `ollama pull` — serwer HTTP uruchamia się automatycznie  
C) Wystarczy samo `ollama serve` — wagi modelu pobierają się przy pierwszym wywołaniu  
D) Trzeba wygenerować klucz API na stronie OpenAI i przekazać go do Ollamy  

**15.** [API] Co się stanie, gdy użyjesz OpenAI SDK bez ustawienia `base_url` przy pracy z Ollamą?

A) Ollama automatycznie przekieruje żądanie na localhost:11434  
B) Biblioteka `requests` przejmie połączenie z lokalnym serwerem  
C) SDK połączy się z chmurą OpenAI i będzie wymagać prawdziwego klucza API  
D) Parametr `model` zostanie zignorowany przez klienta  

**16.** [Python] Co robi parametr `strip=True` w metodzie `soup.get_text()`?

A) Usuwa tagi `script`, `style` i `noscript` z drzewa DOM  
B) Obcina białe znaki z każdego wyciągniętego fragmentu tekstu  
C) Waliduje pola `scheme` i `netloc` adresu URL  
D) Wymusza odpowiedź modelu w formacie punktów  

**17.** [GenAI] Jakie trzy warstwy wyróżnia modularny pipeline w ćwiczeniach kursu?

A) Scraping, fine-tuning, deployment  
B) Embeddings, vector DB, chunking  
C) Frontier, open-source, multimodal  
D) Warstwa danych, warstwa LLM, warstwa orchestracji  

**18.** [GenAI] Dlaczego pipeline streszczania kończy się wyjątkiem `ValueError`, gdy po czyszczeniu HTML tekst strony jest pusty?

A) Aby nie wysyłać pustego promptu do modelu — model bez danych może wygenerować halucynację  
B) Bo Ollama wymaga minimum 12 000 znaków w wiadomości `user`  
C) Bo BeautifulSoup nie obsługuje pustych dokumentów HTML  
D) Bo OpenAI SDK odrzuca wiadomości z rolą `system`  

**19.** [Python] Po co po `get_text()` filtruje się puste linie przed zwróceniem tekstu do modelu?

A) Aby automatycznie walidować poprawność adresu URL  
B) Aby uzyskać czytelniejszy tekst wejściowy dla modelu  
C) Aby wymusić pobranie plików wag modelu z Ollamy  
D) Aby zwiększyć okno kontekstu lokalnego modelu  

**20.** [Narzędzia] Dlaczego Ollama umożliwia użycie tego samego kodu OpenAI SDK co z chmurą OpenAI?

A) Bo Ollama automatycznie tłumaczy odpowiedzi modelu na język polski  
B) Bo Ollama wymaga identycznego klucza API co OpenAI  
C) Bo wystawia endpoint API kompatybilny z OpenAI Chat Completions  
D) Bo parsuje HTML stron przed wysłaniem treści do modelu  
