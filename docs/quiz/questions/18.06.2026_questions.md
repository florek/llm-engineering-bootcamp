# Quiz — 18.06.2026

## Pytania

**1.** [LLM] Co jest pierwszym krokiem integracji treści strony WWW z modelem, gdy model nie ma dostępu do internetu?

A) Fine-tuning modelu na danych strony  
B) Pobranie strony, oczyszczenie HTML i przekazanie tekstu w prompcie  
C) Wdrożenie bazy wektorowej  
D) Function calling do przeglądarki  

**2.** [API] Do czego służy rola `assistant` w tablicy `messages`?

A) Instrukcje globalne dla modelu  
B) Dane wejściowe od użytkownika  
C) Wywołania zewnętrznych narzędzi  
D) Poprzednie odpowiedzi modelu w historii konwersacji  

**3.** [Python] Który parser BeautifulSoup jest wbudowany w Pythona i nie wymaga dodatkowych zależności systemowych?

A) `html.parser`  
B) `lxml`  
C) `html5lib`  
D) `pyquery`  

**4.** [LLM] Czym jest inference w kontekście kursu LLM Engineering?

A) Dostosowanie wag modelu do zadania  
B) Wyszukiwanie dokumentów w bazie wektorowej  
C) Podstawowe wywołanie modelu (chat completion)  
D) Autonomiczna pętla planowania agenta  

**5.** [Narzędzia] Co się stanie po uruchomieniu `ollama serve` bez wcześniejszego `ollama pull` dla danego modelu?

A) Model zostanie automatycznie pobrany z internetu  
B) Serwer działa, ale model bez pobranych wag nie będzie dostępny do inference  
C) Ollama odrzuci wszystkie żądania z kodem 503  
D) SDK automatycznie przełączy się na API OpenAI  

**6.** [API] Jak bezpiecznie obsłużyć sytuację, gdy `message.content` w odpowiedzi modelu jest `None`?

A) Wywołać ponownie `chat.completions.create()` bez parametru model  
B) Odczytać `response.choices[0].text` zamiast message  
C) Zignorować — SDK nigdy nie zwraca None  
D) Użyć `response.choices[0].message.content or ""`  

**7.** [LLM] Która z poniższych korzyści dotyczy lokalnego inference przez Ollamę?

A) Automatyczny dostęp modelu do internetu w czasie rzeczywistym  
B) Większe okno kontekstu niż modele frontier  
C) Dane nie opuszczają maszyny — lepsza prywatność  
D) Brak konieczności uruchomienia lokalnego serwera  

**8.** [Python] Co robi parametr `separator="\n"` w `soup.get_text(separator="\n", strip=True)`?

A) Oddziela tekst z kolejnych bloków HTML nową linią  
B) Usuwa wszystkie znaki nowej linii z wyniku  
C) Zamienia tagi script na znaki nowej linii  
D) Łączy wszystkie linie w jeden ciąg bez spacji  

**9.** [RAG] Jakie podejście jest lepsze niż proste obcięcie tekstu od początku w produkcyjnym systemie na długich dokumentach?

A) Wysyłanie surowego HTML bez czyszczenia  
B) Zwiększenie limitu do pełnej długości strony  
C) Pominięcie wiadomości systemowej  
D) Chunking, map-reduce lub RAG z wyszukiwaniem istotnych fragmentów  

**10.** [LLM] Czym są AI Agents w kontekście kursu?

A) Wyłącznie modele multimodalne przetwarzające audio  
B) Autonomiczne systemy łączące LLM z narzędziami, pamięcią i pętlą planowania  
C) Interfejsy Gradio do czatu z użytkownikiem  
D) Protokoły HTTP do komunikacji z Ollamą  

**11.** [API] Jaki jest typowy wzorzec ułożenia wiadomości w zadaniach produkcyjnych?

A) Tylko `user` bez `system`  
B) Najpierw `assistant`, potem `user`  
C) Najpierw `system` (reguły), potem `user` (dane wejściowe)  
D) Wyłącznie `system` bez treści użytkownika  

**12.** [Python] Do czego służy parametr `timeout=30` w `requests.get()` przy pobieraniu stron?

A) Zapobiega wiszącym połączeniom na wolnych lub nieodpowiadających serwerach  
B) Ogranicza długość pobranego HTML do 30 KB  
C) Ustawia limit 30 sekund na odpowiedź modelu LLM  
D) Automatycznie ponawia żądanie co 30 sekund  

**13.** [Python] Co robi metoda `decompose()` na tagu BeautifulSoup?

A) Konwertuje tag na czysty tekst bez usuwania z DOM  
B) Usuwa tag wraz z całym poddrzewem dzieci z drzewa DOM  
C) Zastępuje tag jego atrybutami jako tekstem  
D) Przenosi tag na koniec dokumentu HTML  

**14.** [LLM] Co oznacza function calling w kontekście LLM?

A) Wywoływanie funkcji Pythona z poziomu parsera HTML  
B) Kompilacja modelu do formatu ONNX  
C) Obcinanie tekstu przed wysłaniem do modelu  
D) Model decyduje, kiedy wywołać zewnętrzną funkcję lub narzędzie  

**15.** [LLM] Czym jest fine-tuning w kontekście kursu?

A) Pierwsze wywołanie modelu przez API  
B) Scraping i czyszczenie stron WWW  
C) Dostosowanie wag modelu do konkretnego zadania  
D) Budowanie interfejsu użytkownika w Gradio  

**16.** [Narzędzia] Jakim typem modelu jest `deepseek-r1:1.5b` używany w ćwiczeniach?

A) Mniejszy model reasoningowy DeepSeek  
B) Największy model frontier w chmurze OpenAI  
C) Model wyłącznie do embeddings  
D) Model multimodalny do analizy obrazów  

**17.** [API] Jaka jest zaleta kompatybilności API Ollamy z OpenAI?

A) Ollama wymaga innego formatu tablicy messages niż OpenAI  
B) Trzeba używać osobnej biblioteki do parsowania odpowiedzi  
C) Odpowiedź ma format JSON niekompatybilny z OpenAI SDK  
D) Struktura odpowiedzi jest identyczna — ten sam kod parsujący działa z chmurą i Ollamą  

**18.** [LLM] W jakim kierunku rozwija się pipeline streszczania pojedynczej strony w kontekście generatora broszur firmowych?

A) Zostaje bez zmian — jedna strona i jedno streszczenie  
B) Wiele stron firmy, chunking + embeddings + vector DB, strukturalny raport  
C) Rezygnacja z modelu na rzecz samego scrapingu  
D) Wyłącznie fine-tuning bez pobierania danych z WWW  

**19.** [Python] Dlaczego po `get_text()` filtrowane są puste linie (`if line.strip()`)?

A) Aby zwiększyć limit tokenów modelu  
B) Bo BeautifulSoup nie obsługuje pustych linii  
C) Aby uzyskać czytelniejszy tekst wejściowy dla modelu  
D) Bo puste linie powodują błąd HTTP 403  

**20.** [Python] Które elementy muszą być obecne w poprawnym URL według walidacji `urlparse` w ćwiczeniu streszczania?

A) Scheme (np. https) i netloc (domena)  
B) Tylko nazwa pliku bez protokołu  
C) Wyłącznie ścieżka po domenie  
D) Fragment hash (#) jako jedyny wymagany element  
