# Quiz — 06.07.2026

## Pytania

**1.** [LLM] Co oznacza skrót RAG w kontekście LLM Engineering?

A) Reinforcement-Augmented Generation  
B) Recursive API Gateway  
C) Retrieval-Augmented Generation  
D) Real-time Automated GPT  

**2.** [API] Co się stanie, gdy użyjesz OpenAI SDK bez ustawienia `base_url` przy pracy z Ollamą?

A) SDK połączy się z chmurą OpenAI i będzie wymagać prawdziwego klucza API  
B) Ollama automatycznie przekieruje żądanie na localhost  
C) Biblioteka `requests` przejmie połączenie z lokalnym serwerem  
D) Parametr `model` zostanie zignorowany  

**3.** [Narzędzia] Na jakim porcie domyślnie nasłuchuje serwer Ollama?

A) 8080  
B) 5000  
C) 3000  
D) 11434  

**4.** [LLM] Które stwierdzenie opisuje modele frontier?

A) Są darmowe i nie generują kosztów per token  
B) Wymagają klucza API i generują koszty per token  
C) Działają wyłącznie lokalnie przez Ollamę  
D) Nie obsługują chat completions  

**5.** [Python] Do czego służy `response.raise_for_status()` po pobraniu strony?

A) Konwertuje HTML na czysty tekst  
B) Ustawia nagłówek User-Agent  
C) Rzuca wyjątek przy kodach HTTP 4xx/5xx  
D) Waliduje poprawność adresu URL  

**6.** [API] Jak działa parametr `api_key` przy połączeniu OpenAI SDK z Ollamą?

A) Jest wymagany przez SDK, ale Ollama nie weryfikuje żadnej wartości  
B) Musi być pobrany z oficjalnej strony Ollamy  
C) Nie jest potrzebny — SDK działa bez niego  
D) Musi dokładnie odpowiadać nazwie modelu  

**7.** [Python] Które tagi HTML są usuwane podczas czyszczenia strony przed wysłaniem do modelu?

A) `div`, `span`, `p`  
B) `script`, `style`, `noscript`  
C) `a`, `img`, `link`  
D) `header`, `footer`, `nav`  

**8.** [LLM] Czym jest Multimodal AI w kontekście kursu?

A) Wyłącznie modele przetwarzające tekst  
B) Modele uruchamiane bez karty graficznej  
C) Modele dostępne tylko przez API frontier  
D) Modele przetwarzające tekst, obraz, audio itd.  

**9.** [RAG] Jaki limit znaków stosowany jest przy obcinaniu treści strony przed wysłaniem do lokalnego modelu w ćwiczeniu streszczania?

A) 1 000  
B) 5 000  
C) 12 000  
D) Bez limitu — wysyłana jest cała strona  

**10.** [API] Jak poprawnie odczytać treść odpowiedzi modelu z obiektu `response`?

A) `response.choices[0].message.content`  
B) `response.text`  
C) `response.body.content`  
D) `response.messages[0].content`  

**11.** [LLM] Z czym związana jest technologia QLoRA wymieniana w kursie?

A) Bazą wektorową do embeddings  
B) Parserem HTML  
C) Biblioteką HTTP do pobierania stron  
D) Fine-tuningiem modeli open-source  

**12.** [Python] Kiedy walidacja URL przez `urlparse` zakończy się błędem w ćwiczeniu streszczania?

A) Gdy URL ma poprawny scheme `https`  
B) Gdy podany jest string bez protokołu (scheme)  
C) Gdy URL zawiera poprawną domenę (netloc)  
D) Gdy URL zawiera ścieżkę po domenie  

**13.** [Narzędzia] Do jakich zadań nadaje się model `llama3.2` według notatek kursu?

A) Jest największym modelem frontier w chmurze  
B) Służy wyłącznie do generowania embeddings  
C) Jest lekkim modelem Meta do chatu i streszczania  
D) Służy wyłącznie do analizy obrazów  

**14.** [LLM] Która kolejność kroków opisuje podstawowy pipeline aplikacji GenAI z kursu?

A) Przygotuj dane → zbuduj prompt → wywołaj model → zwróć wynik  
B) Wywołaj model → pobierz dane → zbuduj prompt → zwróć wynik  
C) Zbuduj prompt → fine-tuning → pobierz dane → zwróć wynik  
D) Wdroż vector DB → wywołaj model → pobierz dane → zwróć wynik  

**15.** [RAG] Dlaczego proste obcięcie tekstu od początku strony jest niewystarczające w produkcyjnym systemie na długich dokumentach?

A) BeautifulSoup nie obsługuje długich plików  
B) Model frontier wymaga surowego HTML  
C) Ollama odrzuca tekst krótszy niż pełna strona  
D) Końcówka artykułu ginie — lepsze są chunking, map-reduce lub RAG  

**16.** [LLM] Czym różni się function calling od zwykłego inference?

A) Function calling wymaga headless browsera  
B) Przy function calling model decyduje, kiedy wywołać zewnętrzną funkcję lub narzędzie  
C) Inference zawsze wymaga bazy wektorowej  
D) Function calling polega wyłącznie na obcinaniu tekstu wejściowego  

**17.** [Python] Dlaczego scraping zwykłym `requests` + BeautifulSoup często zawodzi na stronach SPA (React, Vue)?

A) SPA zawsze zwracają kod 403 Forbidden  
B) BeautifulSoup nie parsuje HTML5  
C) SPA często zwracają pusty HTML — treść renderuje JavaScript po stronie klienta  
D) SPA wymagają parametru `timeout` większego niż 30 sekund  

**18.** [API] Co trzeba zmienić w kodzie, aby przełączyć się z `llama3.2` na `deepseek-r1:1.5b` bez refaktoryzacji?

A) Wyłącznie parametr `model` — klient, messages i parsowanie pozostają bez zmian  
B) Cały obiekt klienta OpenAI i endpoint `base_url`  
C) Format tablicy `messages` na format specyficzny dla DeepSeek  
D) Bibliotekę z OpenAI SDK na osobny klient Ollamy  

**19.** [LLM] Jaki jest skutek wysłania do modelu pustej treści strony (bez wcześniejszej walidacji)?

A) Model automatycznie pobierze stronę z internetu  
B) SDK zwróci błąd walidacji przed wysłaniem żądania  
C) Ollama przełączy się na model frontier  
D) Model może wygenerować halucynację na podstawie pustego promptu  

**20.** [RAG] Jak rozwija się pipeline streszczania pojedynczej strony w kierunku generatora broszur firmowych?

A) Pozostaje bez zmian — jedna strona i jedno streszczenie  
B) Wiele stron firmy, chunking + embeddings + vector DB, strukturalny raport  
C) Rezygnacja z modelu na rzecz samego scrapingu  
D) Wyłącznie fine-tuning bez pobierania danych z WWW  
